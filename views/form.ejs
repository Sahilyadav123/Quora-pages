<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>Create new post</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f7f7;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        form {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 300px;
        }
        input[type="text"],
        textarea {
            width: calc(100% - 20px);
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
            font-size: 14px;
        }
        textarea {
            resize: vertical;
        }
        button {
            background-color: #007bff;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
        }
        button:hover {
            background-color: #0056b3;
        }
        body {
    background-color: #1a1a2e; /* Dark blue background */
    color: #fff; /* Text color */
    font-family: Arial, sans-serif; /* Font family */
}

form {
    background-color: #222; /* Dark gray background for the form */
    padding: 20px; /* Padding */
    border-radius: 8px; /* Border radius */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3); /* Box shadow */
}

input[type="text"],
textarea {
    width: calc(100% - 20px); /* Width */
    padding: 10px; /* Padding */
    margin-bottom: 10px; /* Margin bottom */
    border: 1px solid #ccc; /* Border */
    border-radius: 4px; /* Border radius */
    box-sizing: border-box; /* Box sizing */
    font-size: 14px; /* Font size */
    background-color: rgb(252, 246, 246); /* Dark gray background */
    color: rgb(8, 39, 235); /* Text color */
}

textarea {
    resize: vertical; /* Vertical resizing */
}

button {
    background-color: #007bff; /* Button background color */
    color: #fff; /* Button text color */
    border: none; /* Border */
    padding: 10px 20px; /* Padding */
    border-radius: 4px; /* Border radius */
    cursor: pointer; /* Cursor */
    font-size: 16px; /* Font size */
}

button:hover {
    background-color: #d0fe00;
    color: black; /* Hover background color */
}

    </style>    
</head>

<body>
    <form action="/posts" method="post">
        <input type="text" placeholder="enter username" name="username">
        <br>
        <br>
        <textarea name="content" id="" cols="30" rows="10" placeholder="Write your post"></textarea>
        <br>
        <br>
        <button>Submit </button>
    </form>
</body>

</html>